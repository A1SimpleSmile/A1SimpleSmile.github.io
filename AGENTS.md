# AGENTS.md

## Commands

- `npm run lint` — runs **oxlint** (not ESLint)
- `npm run build` — production build via Vite
- `npm run dev` — dev server with HMR
- `npm run preview` — preview production build

No test runner or typecheck is configured.

## Key Facts

- React 19 + Vite 8, JSX (not TypeScript)
- Linter config: `.oxlintrc.json` (plugins: `react`, `oxc`)
